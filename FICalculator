// average lifetime in US = 78
// important variables: age, current net worth, income, expenses, savings rate
// asset allocation % and expected rate of return


// financial independence number = yearly spending / safe withdrawal rate
// years to FI ^ = (FI number - amount already saved) / yearly saving
// financial experts say 4% is a reasonable SWR for most people

#include <iostream>
using namespace std;

double calculations(int A, double N, double I, double E, double S) {

}

int main() {
    int lifetime = 78;
    double swr = 0.04;
    
    // variables
    double networth, income, expenses, savings, srate;
    int age;
    string answer;
    
    cout << "Would you like to know your ideal retirement age? (y/n): ";
    cin >> answer;
    if (answer == "y" or answer == "Y") {
        // user inputs variables
        cout << "How old are you?: ";
        cin >> age;
        cout << "What is your current net worth?: ";
        cin >> networth;
        cout << "How much do you have in savings?: ";
        cin >> savings;
        cout << "What is your current income?: ";
        cin >> income;
        cout << "What percentage do you plan to save a year?: ";
        cin >> srate;
        cout << "How much are your current expenses?: ";
        cin >> expenses;
        
    }
    
    
    
    
}
